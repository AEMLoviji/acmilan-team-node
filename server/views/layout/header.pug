header
  .jumbotron.hidden-xs
  // jumbotron
  nav.navbar.navbar-inverse
    .container
      .navbar-header
        button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar-collapse', aria-expanded='false')
          span.sr-only Toggle navigation
          span.icon-bar
          span.icon-bar
          span.icon-bar
        a.navbar-brand(href='/')=title
      #navbar-collapse.collapse.navbar-collapse
        ul.nav.navbar-nav.navbar-right
          li
            a(href='/') Home
          li
            a.dropdown-toggle(href='./players.html', data-toggle='dropdown', role='button', aria-haspopup='true', aria-expanded='false')
              | Players
              span.caret
                span
            ul.dropdown-menu
              li
                a(href='/players') All Players
              li.divider(role='separator')
              each player in playersNames
                li
                  a(href=`/players/${player.name}`)=player.name

          li
            a(href='/goal') Goals
        // nav
      // navbar-collapse
    // container